import "./style.css";
import { animate, stagger, inView, scroll, timeline } from "motion";



document.querySelectorAll(".section").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });


  document.querySelectorAll(".section1").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });


  
  document.querySelectorAll(".section2").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });

  
  document.querySelectorAll(".section3").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });

  
  document.querySelectorAll(".section4").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });

  
  document.querySelectorAll(".section5").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });

  
  document.querySelectorAll(".section6").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });

  
  document.querySelectorAll(".section7").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });

  
  document.querySelectorAll(".section8").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });

  
  document.querySelectorAll(".section9").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });

  
  document.querySelectorAll(".section10").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });

  
  document.querySelectorAll(".section11").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });

  
  document.querySelectorAll(".section12").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });

  
  document.querySelectorAll(".section13").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });

  
  document.querySelectorAll(".section14").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });

  document.querySelectorAll(".section15").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });

  document.querySelectorAll(".section16").forEach((section) => {
    const header = section.querySelector("img");
    scroll(animate(header, { y: [-100, 450] }), {
      target: header
    });
  });